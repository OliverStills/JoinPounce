/**
 * /web/components/
 *
 * This directory will contain shared React components for the JoinPounce web app.
 * Populated during Mission 4 (Landing Page) and beyond.
 *
 * Planned components:
 *   - ItemCard.tsx         — Product card: image, name, prices, % saved badge, sparkline
 *   - PriceSparkline.tsx   — 90-day price history chart (recharts or chart.js)
 *   - WaitlistForm.tsx     — Email capture form with loading/success state
 *   - RetailerLogo.tsx     — Retailer logo with proper alt text
 *   - Navbar.tsx           — Site navigation
 *   - Footer.tsx           — Site footer with links
 */

// Placeholder export to keep this a valid TypeScript file
export { };
